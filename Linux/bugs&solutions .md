> ## **无法全屏**：

```
solution：
	1:对于Linux虚拟机，可手动安装Vmware Tools
	2:对于Linux较高发行版，使用集成的open-vm-tools版本可解决--
		一：终端输入sudo apt-get install open-vm-tools
		二：安装依赖，这不很关键 sudo apt-get install open-vm*
		三：reboot，然后可通过Ctrl+shift+enter实现全屏
```

